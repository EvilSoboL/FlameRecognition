FlameRecognition/
├── data/
│   ├── images/
│   │   ├── 200_800 - первое число - расход вводимого пара (г/с), второе - расход дизельного топлива (г/с)
│   │   ├──...
│   ├── data.csv         # Полный список всех изображений и меток
│   ├── train.csv        # Разделение для обучения
│   ├── val.csv          # Разделение для валидации
│   └── test.csv         # Разделение для тестирования
├── scripts/
│   ├── check_data_loading.py  # Проверка загрузки данных
│   ├── prepare_csv.py         # Подготовка data.csv, test.csv, val.csv
│   ├── train_model.py         # Обучение модели


## 🔍 Предсказание (Inference)

После завершения обучения модели вы можете запускать предсказания для изображений пламени с помощью скрипта `scripts/infer_model.py`.

### ✅ Примеры запуска:

**По умолчанию используется EfficientNet:**

```bash
python scripts/infer_model.py path/to/image1.jpg path/to/image2.jpg
```

**Использование альтернативной модели (DenseNet):**

```bash
python scripts/infer_model.py path/to/image.jpg --model best_model_densenet.pth
```

### ⚙️ Параметры:

| Аргумент           | Описание                                                       |
|--------------------|----------------------------------------------------------------|
| `images`           | Пути к изображениям для предсказания (можно несколько подряд). |
| `--model`          | (Необязательный) Путь к `.pth` файлу с весами модели.          |

### 📦 Вывод:
Для каждого изображения будет выведено два значения:
- `fuel_flow` – прогнозируемый расход топлива, г/с
- `diluent_flow` – прогнозируемый расход разбавителя, г/с